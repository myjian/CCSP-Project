<div id="body" class="container-fluid">
    <p style = "text-align: left;">有*的欄位為必填</p>
    <form action="/userinfo" method="post" onsubmit="return validateForm()">
        <div id="reportInfo">
            <div class="form-group">
                <label>*真實姓名</label>
                <input class="form-control" type="text" name="name" value = "<%= userInfo.name %>" placeholder="真實姓名可提高檢舉成功機率。">
            </div>
            <div class="form-group">
                <label>*電話</label>
                <input class="form-control" type="text" name="phone" value = "<%= userInfo.phone %>" placeholder="電話真實性會影響檢舉是否成功">
            </div>
            <div class="form-group">
                <label>*地址</label>
                <input class="form-control" type="text" name="address" value = "<%= userInfo.address %>" placeholder="地址真實性會影響檢舉是否成功">
            </div>
            <div class="form-group">
                <label>*電子郵件信箱</label>
                <input class="form-control" type="text" name="email" value = "<%= userInfo.email %>" placeholder="收到回信的信箱。">
            </div>
            <div class="form-group">
                <label>性別</label>
                <input class="form-control" type="text" name="gender" value = "<%= userInfo.gender %>" placeholder="檢舉發生在台東縣的案件需要性別">
            </div>
            <div class="form-group">
                <label>身分證字號</label>
                <input class="form-control" type="text" name="idcardnumber" value = "<%= userInfo.idCardNumber %>" placeholder="檢舉發生在新竹市的案件需要身分證字號">
            </div>
            <div class="form-group">
                <input class="form-control btn-success" type="submit" value="確認送出">
                <a class="btn btn-default btn-block" href="/">取消</a>
            </div>
        </div>
    </form>  
    <div id="information">
    </div>
</div>
<script type="text/javascript" src="js/baddriver.js"></script>
<script type="text/javascript" src="js/filltable.js"></script>
